import json
from pathlib import Path

from docx import Document

from build_delivery1_reports import (
    add_bullets,
    add_callout,
    add_heading,
    add_numbered,
    add_para,
    add_table,
    setup_doc,
)


ROOT = Path(__file__).resolve().parent
OUT = ROOT / "交付2-MVP原型功能测试报告"
RESULTS = ROOT / "mvp_function_test_results.json"


def yn(value):
    return "通过" if value else "失败"


def build_report():
    data = json.loads(RESULTS.read_text(encoding="utf-8"))
    doc = setup_doc(
        "MVP原型功能测试报告",
        "百度办公 AI 助手｜功能完整性、核心流程、缺陷与修复建议",
    )
    add_callout(
        doc,
        "测试结论",
        "本轮自动化冒烟测试共执行16项，15项通过、1项失败，通过率93.75%。核心内容生成、编辑、导出、保存、历史记录、反馈和数据存储流程可用；任务自动化在千帆接口不可用时缺少降级机制。综合判定为“有条件通过”。",
    )

    add_heading(doc, "一、测试背景与目标")
    add_para(doc, "百度办公AI助手MVP已经形成较完整的办公内容处理流程，包含资料输入、智能推荐、会议纪要、文档问答、任务拆解、周报生成、自然语言咨询、任务自动化、编辑确认、结果导出、历史记录、我的文件、用户反馈和数据指标等功能。为判断当前原型是否满足第10周功能完整性要求，需要对各功能入口、核心流程、异常处理和数据记录能力进行统一测试。")
    add_para(doc, "本次测试的目标是确认MVP能够正常启动，用户能够完成从资料输入到结果导出的主要流程，各项功能能够给出明确反馈，关键事件能够写入数据库，并识别影响演示、验收和后续AI专项测试的问题。测试结果用于形成Bug列表和修复建议，不代替后续不少于50个用例的AI能力效果专项测试。")

    add_heading(doc, "二、测试依据与范围")
    add_heading(doc, "2.1 测试依据", 2)
    add_para(doc, "本次测试依据第10周任务中“按照PRD验收标准，对MVP原型的所有功能进行全面测试”的要求开展。当前工作区未提供独立PRD原文，因此验收点主要根据现有app.py实现、页面说明、已形成的数据看板指标以及MVP核心业务流程整理。后续如补充正式PRD，应将本报告中的验收项与PRD逐条映射并补测差异项。")
    add_heading(doc, "2.2 测试范围", 2)
    add_table(doc, ["测试模块", "主要测试内容", "是否纳入"], [
        ["启动与导航", "应用启动、首页展示、侧边导航和页面切换", "是"],
        ["资料输入", "首页粘贴、上传入口、空输入校验和多格式支持", "是"],
        ["智能推荐", "根据资料推荐会议纪要、文档问答、任务拆解或周报生成", "是"],
        ["内容生成", "四项内容生成、结果展示、重新生成和错误降级", "是"],
        ["AI咨询", "咨询页面、多轮消息状态和接口异常处理", "是"],
        ["任务自动化", "结构化任务生成、状态更新、进度和任务导出", "是"],
        ["编辑与导出", "编辑保存、Word/PDF/TXT生成、我的文件", "是"],
        ["记录与反馈", "历史记录、用户反馈、SQLite事件和指标页面", "是"],
        ["AI内容质量", "准确性、相关性、安全性、流畅性和有用性", "仅做功能级检查，另行专项测试"],
        ["并发与压力", "高并发、长时间运行和大规模文件压力", "本轮不纳入"],
    ], [1900, 5600, 1860], 8.8)

    add_heading(doc, "三、测试环境与方法")
    add_heading(doc, "3.1 测试环境", 2)
    add_table(doc, ["项目", "测试配置"], [
        ["测试日期", "2026年8月12日"],
        ["应用技术", "Python、Streamlit 1.50.0、SQLite"],
        ["测试对象", "D:\\PycharmProjects\\PythonProject6\\app.py"],
        ["Python环境", "项目.venv环境"],
        ["AI接入", "文心千帆接口；接口异常时部分功能使用本地演示结果"],
        ["测试数据", "独立临时数据库，不修改项目原有mvp_metrics.db"],
        ["测试方式", "Streamlit AppTest自动化冒烟测试＋源码与数据结构核验"],
    ], [2200, 7160])
    add_heading(doc, "3.2 测试方法", 2)
    add_numbered(doc, [
        "启动应用并检查页面是否存在未处理异常；",
        "使用代表性中文办公文本执行主流程，验证页面状态和结果内容；",
        "依次切换四项内容生成功能，检查结果是否正常返回；",
        "测试编辑、保存、Word/PDF/TXT导出、历史记录和我的文件；",
        "测试任务自动化、数据指标和反馈提交；",
        "检查临时SQLite数据库的表结构、事件数量和反馈写入结果；",
        "结合源码核验未被自动化工具直接操作的上传格式、异常处理和持久化逻辑。",
    ])

    add_heading(doc, "四、验收标准与缺陷等级")
    add_heading(doc, "4.1 功能验收标准", 2)
    add_table(doc, ["结果", "判定条件"], [
        ["通过", "功能入口可访问，主要操作可完成，结果或提示符合预期，未出现阻断性异常"],
        ["失败", "功能无法完成、页面出现未处理异常、结果为空或核心数据未正确保存"],
        ["阻塞", "受外部接口、权限或测试数据限制，当前无法完成有效验证"],
        ["未执行", "不属于本轮范围或缺少必要测试条件"],
    ], [1500, 7860])
    add_heading(doc, "4.2 缺陷等级", 2)
    add_table(doc, ["等级", "定义", "处理要求"], [
        ["严重", "系统无法启动、数据泄露或所有核心流程不可用", "停止交付，立即修复"],
        ["高", "核心功能不可用且没有可行替代流程", "验收前优先修复并回归"],
        ["中", "功能可用但结果、数据或异常处理存在明显问题", "纳入当前迭代修复"],
        ["低", "轻微体验、兼容性或提示问题，不阻断主流程", "排期优化并回归"],
    ], [1300, 5460, 2600])

    add_heading(doc, "五、测试结果概览")
    add_table(doc, ["统计项", "结果"], [
        ["自动化测试用例", str(data["total"])],
        ["通过", str(data["passed"])],
        ["失败", str(data["failed"])],
        ["自动化通过率", f"{data['passed'] / data['total'] * 100:.2f}%"],
        ["综合结论", "有条件通过"],
    ], [3200, 6160])
    add_para(doc, "测试表明，应用能够正常启动，首页输入、功能选择、四项办公内容生成、编辑确认、三种格式导出、保存到我的文件、历史记录、反馈提交和SQLite数据写入均能完成。唯一直接失败的自动化用例为任务自动化：测试环境检测到千帆凭证，但接口请求未成功，系统未使用本地降级任务，最终任务数量为0。")
    add_callout(doc, "解释说明", "本轮四项内容生成虽然成功显示了本地结果，但由于千帆调用失败，事件日志将请求记录为generation_failed，导致数据指标页面显示生成成功率为0.0%。这属于指标口径和埋点问题，不代表用户没有获得结果。", "FFF4E5")

    add_heading(doc, "六、详细功能测试结果")
    rows = []
    for item in data["results"]:
        detail = item["detail"]
        if len(detail) > 95:
            detail = detail[:92] + "……"
        rows.append([item["case_id"], item["module"], item["action"], yn(item["passed"]), detail])
    add_table(doc, ["编号", "模块", "测试内容", "结果", "实际结果摘要"], rows,
              [1000, 1500, 2850, 900, 3110], 8.1)

    add_heading(doc, "6.1 补充源码核验", 2)
    add_table(doc, ["编号", "核验内容", "核验结果", "说明"], [
        ["SV-001", "TXT、Markdown文本读取", "通过", "按UTF-8、GB18030、UTF-16顺序尝试解码"],
        ["SV-002", "PDF和DOCX正文提取", "通过（有限制）", "支持文本型PDF和普通DOCX；扫描型PDF无OCR"],
        ["SV-003", "多文件上传", "通过", "允许同时选择多个PDF、DOCX、TXT和MD文件并合并正文"],
        ["SV-004", "空资料校验", "通过", "无可读正文时阻止进入下一步并显示提示"],
        ["SV-005", "历史记录搜索与删除", "通过", "支持关键词、类型筛选、查看、删除和清空"],
        ["SV-006", "任务状态更新与任务清单导出", "阻塞", "页面逻辑存在，但本轮任务生成失败，无法执行完整运行验证"],
        ["SV-007", "隐私提示", "通过", "上传页和帮助页提示不要上传敏感信息，指标日志不保存正文"],
        ["SV-008", "千帆真实生成效果", "未执行", "当前接口调用失败；准确性等内容质量应在交付物3中专项测试"],
    ], [1100, 2600, 1500, 4160], 8.4)

    add_heading(doc, "七、Bug列表与修复建议")
    add_table(doc, ["Bug编号", "等级", "问题描述", "影响", "修复建议"], [
        ["BUG-001", "高", "千帆凭证存在但接口调用失败时，任务自动化直接报错且不生成任务。", "任务自动化核心流程不可用。", "为build_automation_tasks增加与内容生成一致的异常降级；失败时生成本地结构化任务，并记录automation_fallback_used。"],
        ["BUG-002", "中", "千帆调用失败但本地结果成功展示时，事件被记录为generation_failed。", "数据指标显示0%成功率，无法区分模型失败与结果返回成功。", "拆分qianfan_failed、local_fallback_used和result_displayed事件；分别计算千帆成功率和总体结果返回率。"],
        ["BUG-003", "中", "AI咨询在千帆调用异常时没有本地降级，只显示咨询失败。", "接口短暂故障会中断咨询流程，体验与内容生成功能不一致。", "在chat_answer外层增加可控降级回复，同时明确标注“本地演示”，避免误认为模型答案。"],
        ["BUG-004", "中", "历史记录和“我的文件”仅保存在session_state中。", "刷新页面、会话结束或服务重启后数据丢失。", "将记录元数据和生成文件持久化到SQLite或受控文件目录，并增加用户/会话隔离和删除能力。"],
        ["BUG-005", "中", "本地演示结果包含通用负责人、时间和结论，未全部标记为“待确认”。", "可能被用户误认为来源于上传资料，存在内容可信度风险。", "仅引用输入中明确出现的信息；推测字段统一标记“待确认/建议值”，并在导出文档中保留风险提示。"],
        ["BUG-006", "低", "扫描型PDF无法提取正文，当前仅表现为没有可用文本。", "用户难以判断是文件为空还是缺少OCR能力。", "检测提取文本为空的PDF并提示“可能为扫描件”；后续接入OCR或要求用户上传可复制文本版本。"],
        ["BUG-007", "低", "Streamlit运行时提示use_container_width将在后续版本移除。", "升级Streamlit后可能产生兼容性问题。", "按组件类型改用width='stretch'或width='content'，完成全页面回归。"],
    ], [1000, 700, 2600, 2200, 2860], 7.8)

    add_heading(doc, "八、修复优先级与回归建议")
    add_heading(doc, "8.1 第一优先级：保证核心流程可用", 2)
    add_bullets(doc, [
        "修复任务自动化的接口失败降级，确保演示和验收时能够生成结构化任务；",
        "修复生成事件埋点，区分千帆失败、本地降级和页面结果返回；",
        "为AI咨询补充异常降级或清晰的重试机制。",
    ])
    add_heading(doc, "8.2 第二优先级：保证数据和内容可信", 2)
    add_bullets(doc, [
        "将历史记录和我的文件持久化，避免会话结束后丢失；",
        "调整本地演示模板，所有非原文信息必须标记为建议值或待确认；",
        "在数据指标页补充千帆成功率、本地降级率、总体结果返回率和任务完成率。",
    ])
    add_heading(doc, "8.3 建议回归用例", 2)
    add_numbered(doc, [
        "断开网络或使用无效千帆配置，验证四项生成、AI咨询和任务自动化的降级行为；",
        "连续执行10次生成，核对请求数、千帆成功数、降级数和结果返回数是否一致；",
        "刷新浏览器并重新建立会话，检查历史记录和我的文件是否仍可访问；",
        "使用含姓名、日期和任务负责人的资料，确认生成结果只引用原文信息；",
        "上传文本型PDF、扫描型PDF、DOCX、TXT和MD，核对提取结果及错误提示；",
        "升级组件参数后回归所有按钮、下载组件、表格和导航布局。",
    ])

    add_heading(doc, "九、风险与测试限制")
    add_bullets(doc, [
        "本轮未完成千帆真实接口的稳定调用，因此无法对真实模型的内容质量作通过结论；",
        "自动化测试采用代表性中文文本，尚未覆盖超长文档、损坏文件、特殊字符文件名和大文件压力；",
        "未进行多人并发、浏览器兼容性和长时间稳定性测试；",
        "当前没有独立PRD原文，验收标准由现有实现和交付要求整理，后续需要与正式PRD补充映射；",
        "AI准确性、相关性、安全性、流畅性和有用性应在《AI能力效果专项测试报告》中使用不少于50个标准化用例进一步验证。",
    ])

    add_heading(doc, "十、测试结论")
    add_para(doc, "本轮测试确认百度办公AI助手MVP的主要内容处理链路已经形成，用户可以输入资料、选择功能、获得会议纪要／文档问答／任务拆解／周报结果、编辑内容并导出Word、PDF或TXT，也可以保存文件、查看历史记录、提交反馈和查看基础数据指标。应用启动、页面切换、表单校验和SQLite写入未出现阻断性异常。")
    add_para(doc, "自动化测试16项中15项通过、1项失败，通过率为93.75%。由于任务自动化在千帆接口不可用时无法降级，同时生成成功率埋点存在口径失真，当前版本判定为“有条件通过”，适合继续进行内部演示和测试，但不建议在修复BUG-001和BUG-002前作为稳定验收版本。完成高、中优先级问题修复并通过回归测试后，可进入AI能力专项测试和竞品对比测试阶段。")

    return doc


def audit(path):
    doc = Document(path)
    text = "\n".join(p.text for p in doc.paragraphs)
    assert "有条件通过" in text
    assert "BUG-001" in "\n".join(c.text for t in doc.tables for row in t.rows for c in row.cells)
    assert len(doc.tables) >= 8
    assert len(doc.paragraphs) >= 45
    return {
        "paragraphs": len(doc.paragraphs),
        "tables": len(doc.tables),
        "sections": len(doc.sections),
        "images": len(doc.inline_shapes),
    }


def main():
    OUT.mkdir(exist_ok=True)
    path = OUT / "MVP原型功能测试报告-完整版.docx"
    build_report().save(path)
    print(path)
    print(audit(path))


if __name__ == "__main__":
    main()
